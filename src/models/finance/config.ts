export type AreaType = {
  id: number;
  created_at: number;
  updated_at: number;
  title: string;
  description: string;
};

export type CategoryType = {
  id: number;
  name: string;
  description: string;
};
